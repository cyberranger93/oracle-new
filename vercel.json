{
  "buildCommand": "cd frontend && CI=false npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "frontend/build",
  "installCommand": "echo 'Skipping root install'",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
}
