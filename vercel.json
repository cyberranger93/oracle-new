{
  "buildCommand": "cd frontend && DISABLE_ESLINT_PLUGIN=true npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "frontend/build",
  "installCommand": "echo 'Skipping root install'"
}
